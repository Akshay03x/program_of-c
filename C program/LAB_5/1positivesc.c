#include<stdio.h>
    void main()
    {
        int a;
        printf("enter number of whether:");
        scanf("%d",&a);

        if(a>0)
        {
            printf("the given number%d is positive",a);
        }


        if(a<0)

        {
            printf("the given number%dis negative",a);
        }
}
